<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>VR02_TS_LockRecord</fullName>
    <active>true</active>
    <description>When the status is accepted (nb TSE accpeted = nb TSE), then the record is locked.</description>
    <errorConditionFormula>ISPICKVAL( Status__c , &quot;Accepted&quot;)  &amp;&amp;(  ISCHANGED( MondayDate__c )  || ISCHANGED(  OwnerId ))</errorConditionFormula>
    <errorMessage>A Timesheet accepted cannot be updated.</errorMessage>
</ValidationRule>
